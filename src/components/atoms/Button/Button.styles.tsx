import styled from "styled-components";

const Button = styled.button`
  background: none;
  color: inherit;
  border: none;
  padding: 0.5rem;
  font: inherit;
  cursor: pointer;
  outline: inherit;
`;

export { Button };
