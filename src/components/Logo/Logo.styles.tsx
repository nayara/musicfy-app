import { styled } from "styled-components";

const Button = styled.button`
  background: none;
  color: inherit;
  border: none;
  padding: 0;
  font: inherit;
  cursor: pointer;
  outline: inherit;
`;

const Logo = styled.h1`
  font-family: "Genty";
  padding: 1rem;
  margin: 0;
`;

export { Logo, Button };
